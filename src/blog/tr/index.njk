---
layout: layouts/base.njk
title: Blog (Turkce)
lang: tr
description: Kisa, haftalik notlar.
---
<section class="page">
  <h1>Blog</h1>
  <div class="page-row">
    <p class="lead">Some thoughts.</p>
    <label class="lang-select" for="blog-lang">
      <span class="sr-only">Language</span>
      <select id="blog-lang" name="lang" onchange="window.location.href=this.value">
        <option value="/blog/">En</option>
        <option value="/blog/tr/" selected>Tr</option>
      </select>
    </label>
  </div>
</section>

<section class="posts">
  {% set posts = collections.post | byLang("tr") | reverse %}
  {% if posts.length == 0 %}
    <p class="lead">Henuz yazi yok.</p>
  {% endif %}
  {% for post in posts %}
  <article class="post-card">
    <p class="post-date">{{ post.data.date | dateLocale("tr") }}</p>
    <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
    {% if post.data.summary %}<p>{{ post.data.summary }}</p>{% endif %}
  </article>
  {% endfor %}
</section>
